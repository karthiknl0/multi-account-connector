{
  "name": "multi-account-connector-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server that brokers multiple OAuth accounts and exposes safe, account-scoped tools.",
  "bin": {
    "mac": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "cli": "node dist/cli.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
